The .ind input files in this directory must have *nix line endings (lf) to work properly with the tests. The comparison
functions expect lf endings -- this is what they get from the application web UI.